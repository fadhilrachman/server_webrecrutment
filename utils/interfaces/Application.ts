import JobInterface from "./job";

interface ApplicationInterface {
  application: string;
  user: string;
  status: string;
  notes: string;
  cover_letter: string;
}

export default ApplicationInterface;
